import { PlaceholderSection } from "@/components/placeholder-section";

export default function SettingsPage() {
  return (
    <PlaceholderSection
      title="Settings"
      description="Account, organization, and workspace settings will be managed here."
    />
  );
}
