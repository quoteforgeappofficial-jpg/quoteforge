import { PlaceholderSection } from "@/components/placeholder-section";

export default function DashboardPage() {
  return (
    <PlaceholderSection
      title="Dashboard"
      description="A summary of recent activity across customers, estimates, and proposals will live here."
    />
  );
}
